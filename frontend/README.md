# Obshak frontend

Vite + React + TypeScript SPA. All UI code is under `src/`.

```bash
npm install
npm run dev
npm run build
npm run lint
```

Production image: `docker compose build app` (build context is this directory).
